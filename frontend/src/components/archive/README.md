# Archived frontend components

Pre–Public Lab v1.0 UI (derivatives workbench, proof dashboard, trading interface, commercial pilot).

**Live app uses only:**
- `PublicLabLanding.jsx` — public pitch landing
- `SpkV1Console.jsx` — wallet + Sepolia payments console

Do not wire these back into `App.jsx` without an explicit product decision.
